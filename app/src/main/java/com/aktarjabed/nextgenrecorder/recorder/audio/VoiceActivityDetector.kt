package com.aktarjabed.nextgenrecorder.recorder.audio

import android.content.Context

class VoiceActivityDetector(private val context: Context) {
    fun isSpeech(samples: ShortArray, samplesRead: Int): Boolean {
        // Placeholder
        return true
    }
}